# Dependencies needed by the Streamlit web demo on Streamlit Cloud.
# Streamlit Cloud uses this file (plus packages.txt for apt-installs) to
# build the app's container.

# --- Core agent dependencies (mirror requirements.txt) ---
pgmpy>=0.1.25
scikit-learn>=1.4.0
pandas>=2.1.0
numpy>=1.26.0
joblib>=1.3.0
requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.5.0
rich>=13.7.0
click>=8.1.0

# --- Streamlit-specific ---
streamlit>=1.32.0
plotly>=5.18.0

# --- LLM SDKs (both supported; user picks one in the UI) ---
anthropic>=0.30.0
openai>=1.12.0
