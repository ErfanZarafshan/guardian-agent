# Copy this file to `.env` in the project root and fill in real values.
# .env is gitignored — do not commit real secrets.

# --- OpenWeatherMap ---
# Sign up at https://openweathermap.org/api -> API keys
OWM_API_KEY=your_openweathermap_api_key_here

# Set to "free" to use the /data/2.5/* endpoints (no card required),
# or "onecall" to use One Call API 3.0 (requires paid subscription setup,
# even though first 1000 calls/day are free).
OWM_MODE=free

# --- NWS API ---
# NWS requires no key but asks for a User-Agent identifying your app + contact.
NWS_USER_AGENT=GuardianAgent/0.1 (your_email@example.com)

# --- Twilio (free trial) ---
# From https://console.twilio.com/
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_FROM_NUMBER=+15551234567

# --- Runtime ---
# Polling cadence for the main agent loop (seconds).
POLL_INTERVAL_SECONDS=300

# Log level: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO
