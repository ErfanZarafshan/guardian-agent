"""Unified weather observation model.

Implemented in **Phase 3**. This is the normalized representation that the
Bayesian network consumes as evidence, regardless of which API it came from.

Fields will include: event_type, severity, urgency, certainty, wind_speed_mph,
precip_rate_in_per_hr, temp_f, visibility_mi, forecast horizon, etc.
"""
